Speaker 1: Building a configuration database has three moves. Start by identifying the configuration items that matter, rather than everything you own.

Speaker 2: That restraint is the hard part. A CMDB that tries to record every object becomes stale faster than anyone can maintain it.

Speaker 1: Then populate the attributes from trusted sources, ideally automated discovery rather than a spreadsheet someone updates by hand.

Speaker 2: And map the relationships between items, because that's what makes it useful. Knowing a server exists is trivia; knowing which service fails when it reboots is impact analysis.
